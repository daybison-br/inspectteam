import { integer, sqliteTable, text } from 'drizzle-orm/sqlite-core';
export const localSession = sqliteTable('local_session', { id: integer('id').primaryKey(), userJson: text('user_json').notNull(), tenantId: text('tenant_id'), lastOnlineAt: text('last_online_at') });
export const tenants = sqliteTable('tenants', { tenantId: text('tenant_id').primaryKey(), membershipId: text('membership_id').notNull(), name: text('name').notNull(), slug: text('slug').notNull(), membershipType: text('membership_type').notNull(), status: text('status').notNull() });
export const forms = sqliteTable('forms', { formId: text('form_id').primaryKey(), tenantId: text('tenant_id').notNull(), name: text('name').notNull(), description: text('description'), versionId: text('version_id').notNull(), version: integer('version').notNull(), definitionJson: text('definition_json').notNull(), publishedAt: text('published_at'), deleted: integer('deleted', { mode: 'boolean' }).notNull().default(false) });
export const submissions = sqliteTable('submissions', { id: text('id').primaryKey(), tenantId: text('tenant_id').notNull(), formId: text('form_id').notNull(), formVersionId: text('form_version_id').notNull(), status: text('status').notNull(), answersJson: text('answers_json').notNull(), revision: integer('revision').notNull().default(0), createdAt: text('created_at').notNull(), updatedAt: text('updated_at').notNull() });
export const attachments = sqliteTable('attachments', { id: text('id').primaryKey(), submissionId: text('submission_id').notNull(), fieldId: text('field_id').notNull(), uri: text('uri').notNull(), name: text('name').notNull(), mime: text('mime').notNull(), size: integer('size'), status: text('status').notNull(), remoteJson: text('remote_json') });
export const mutationQueue = sqliteTable('mutation_queue', { id: text('id').primaryKey(), mutationId: text('mutation_id').notNull().unique(), tenantId: text('tenant_id').notNull(), submissionId: text('submission_id').notNull(), operation: text('operation').notNull(), status: text('status').notNull(), attempts: integer('attempts').notNull().default(0), error: text('error'), createdAt: text('created_at').notNull() });
export const syncState = sqliteTable('sync_state', { tenantId: text('tenant_id').primaryKey(), cursor: text('cursor'), lastSyncedAt: text('last_synced_at') });
