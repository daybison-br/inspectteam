"use client";/* eslint-disable @typescript-eslint/no-explicit-any */
import {FormEvent,useEffect,useState} from "react";import {api,json} from "@/app/lib/api";import {Icons,Notice} from "./DesignSystem";
export default function ProfileSettings(){const [me,setMe]=useState<any>(),[saved,setSaved]=useState(false);useEffect(()=>{api("me").then(setMe)},[]);async function save(e:FormEvent<HTMLFormElement>){e.preventDefault();const updated=await api("me",json("PATCH",Object.fromEntries(new FormData(e.currentTarget))));setMe(updated);setSaved(true)}return <form className="panel" onSubmit={save}><div><span className="eyebrow">Conta pessoal</span><h2>Seu perfil</h2><p className="muted">Este nome identifica suas ações e registros de auditoria.</p></div>{saved&&<Notice tone="success">Perfil atualizado.</Notice>}<label>Nome<input name="displayName" defaultValue={me?.displayName??""} required/></label><label>E-mail<input value={me?.email??""} disabled/></label><button className="primary"><Icons.save/>Salvar perfil</button></form>}
